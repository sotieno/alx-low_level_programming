#include "main.h"

/**
 * mul - multiplies two integers
 * @a: first character
 * @b: second character
 * Return: 1 if c is digit, 0 otherwise
 */

int mul(int a, int b)
{
	int product = a * b;

	return (product);
}
